# Linear CLI Cursor Rules

This directory contains Metric Insights guidance for using Linear CLI inside Cursor.

## Skills

- `.cursor/skills/ai-development/SKILL.md` — development (Linear dev gate, greenfield pp-dev, Figma, PR, DoD).
- `.cursor/skills/ai-testing/SKILL.md` — testing (Linear test gate, UI check, test-documentation format).
- `.cursor/skills/figma-code-connect/SKILL.md` — Figma **Code Connect** template authoring (`.figma.ts`). Requires Figma MCP (see `.mcp.json` / `.cursor/mcp.json`).

## Files

- `linear-cli.mdc` — global Linear CLI behavior.
- `linear-task-gates.mdc` — mandatory issue state gates (`Waiting AI Test` / `Waiting AI Development`).
- `portal-env-credentials.mdc` — `.env` variables for instance URL and Portal login (never commit secrets).
- `test-case-rules.mdc` — pointers to **ai-testing** for test documentation.
- `test-suite-template.mdc` — page test-suite structure (see **ai-testing** skill).
- `figma-mcp.mdc` — Figma MCP design-to-code implementation rules. _(Present only when Figma MCP was enabled at bootstrap.)_

The repository root may also contain **`.cursorrules`** (legacy single-file hook). It points at this directory and at **`AGENTS.md`** (Claude agent index — generated by the same bootstrapper); structured rules stay in **`*.mdc`** files above.

## Portal Page / Custom App context

Repositories using this setup are **Portal Page / Custom App** projects: local dev runs at **`http://localhost:<port>`** (default **3000**, then **3001**, **3002**, … if busy). Read **`pp-dev.config`** for the configured port and tooling; dev commands include **`npm run dev`**, **`npx pp-dev`**, and for Next.js **`npx pp-dev next`**. Backend API access is usually via **`.env`**. Full setup steps live in the generated **`setup-cursor-assistant.md`** (or **`setup-claude-assistant.md`** when using Claude). **`LINEAR_CLI.md`** at the repo root documents **`linear-cli`** commands.

## Notes

- These rules are generated by `@metricinsights/ca-ai-tools-setup`.
- Re-run the generator with `--force` to overwrite existing rule files.
