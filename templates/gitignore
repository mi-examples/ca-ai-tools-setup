# ca-ai-tools-setup
# Generated per developer machine — never commit it. It records the local instance
# URL, shell profile and paths, which differ for everyone and leak environment detail.
.dev-environment.md
